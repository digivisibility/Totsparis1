name: tots_paris
description: Kids brand.
publish_to: 'none'

version: 1.0.0+6

environment:
  sdk: ^3.5.3
  flutter: 3.24.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # UI & Design
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  percent_indicator: ^4.2.3
  flutter_feather_icons: ^2.0.0
  nb_utils: ^7.0.5
  font_awesome_flutter: ^10.7.0
  iconly: ^1.0.1
  syncfusion_flutter_sliders: ^31.2.15
  carousel_slider: ^5.0.0
  readmore: ^3.0.0
  flutter_slidable: ^4.0.3
  timeline_tile: ^2.0.0
  shimmer: ^3.0.0
  blurrycontainer: ^2.1.0
  flutter_staggered_grid_view: ^0.7.0
  flutter_image_slideshow: ^0.1.6
  community_material_icon: ^5.9.55

  # Core Services
  dio: ^5.7.0
  http: ^1.2.2
  intl: ^0.20.2
  shared_preferences: ^2.3.2
  flutter_riverpod: ^2.5.3
  provider: ^6.1.2
  internet_connection_checker: ^3.0.1

  # Push Notifications (Updated for iOS 17/18 Stability)
  onesignal_flutter: ^5.3.5
  
  # Firebase (Updated for late 2025/2026 compatibility)
  firebase_core: ^4.3.0
  firebase_auth: ^5.3.0
  google_sign_in: ^6.2.1

  # Payment Gateways
  flutter_stripe: ^12.1.1
  flutter_paypal: ^0.2.1
  flutter_tap_payment: ^0.2.1
  flutter_sslcommerz: 2.4.4
  razorpay_flutter: ^1.3.3

  # Utils & Media
  flutter_easyloading: ^3.0.5
  flutter_countdown_timer: ^4.1.0
  webview_flutter: ^4.0.0
  share_plus: ^12.0.1
  flag: ^7.0.0
  flutter_sound: ^9.16.3
  image_picker: ^1.0.7
  pinput: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - images/

flutter_intl:
  enabled: true
