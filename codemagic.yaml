workflows:
  ios-release:
    name: iOS App Store Release
    instance_type: mac_mini_m2
    max_build_duration: 120 # Increased duration for complex pod installs
    
    integrations:
      app_store_connect: Tots Paris1 

    environment:
      groups:
        - apple_auth       
        - firebase_config  
      vars:
        BUNDLE_ID: "com.dv.totsparis"
      flutter: 3.24.4     
      xcode: 16.1         
      cocoapods: default

    scripts:
      - name: Set iOS Deployment Target to 15.0
        script: |
          # Programmatically force the project and Podfile to iOS 15.0
          # This resolves the 'firebase_core requires a higher minimum version' error
          sed -i '' "s/^# platform :ios, '.*'/platform :ios, '15.0'/" ios/Podfile
          sed -i '' "s/^platform :ios, '.*'/platform :ios, '15.0'/" ios/Podfile
          find ios/Runner.xcodeproj/project.pbxproj -type f -exec sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = [0-9.]*;/IPHONEOS_DEPLOYMENT_TARGET = 15.0;/g' {} +

      - name: Set up keychain
        script: |
          keychain initialize

      - name: Load Firebase configuration
        script: |
          #!/usr/bin/env sh
          set -e
          mkdir -p $CM_BUILD_DIR/ios/Runner
          echo "$IOS_FIREBASE_SECRET" > $CM_BUILD_DIR/ios/Runner/GoogleService-Info.plist

      - name: Fetch signing files
        script: |
          app-store-connect fetch-signing-files "$BUNDLE_ID" --type IOS_APP_STORE --create

      - name: Add certificates to keychain
        script: |
          keychain add-certificates

      - name: Set up code signing settings
        script: |
          xcode-project use-profiles

      - name: Install dependencies
        script: |
          flutter packages pub get
          cd ios
          # Clean old artifacts and install with repo update for latest Firebase/OneSignal
          rm -rf Pods Podfile.lock
          pod install --repo-update
          cd ..

      - name: Build iOS IPA
        script: | 
          # Use --release (not --debug) for App Store/TestFlight
          flutter build ipa --release \
            --bundle-name="Tots Paris" \
            --build-name=1.0.$PROJECT_BUILD_NUMBER \
            --build-number=$PROJECT_BUILD_NUMBER \
            --export-options-plist=$HOME/export_options.plist
            
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
